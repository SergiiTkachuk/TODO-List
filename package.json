{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "This is a simple console application that allows you to track your progress, important events or just reminders, \r breaking everything down into specific tasks. The json file is used to store tasks. Certain commands are used to manage jobs.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.1"
  }
}
